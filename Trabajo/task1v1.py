resultado = []
tree = []

txtTree = open("piramide.txt", "r")

for lines in txtTree:
    lines = lines.rstrip()  
    characters = lines.split(" ")
    tree.append(characters)

where = 0
resultado.append(tree[0][0])

def sum(x, y):
    return tree[x,]

for y in range(0, len(tree)-1):
    for x in range(0, len(tree[y])):
        r = []
        for j in range(0, len(resultado[y])):


print (r)